# SECURITY

## Quy tắc an toàn

- Không lưu token/API key/password/private key trong repo.
- Không in secret ra log.
- Dùng .gitignore để chặn file nhạy cảm.
- Dùng Gitleaks trước commit/push.
- Nếu phát hiện secret thì dừng, không commit, không push.
- RooCode hỏi xác nhận trước commit/push.
